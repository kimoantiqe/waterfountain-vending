*.iml
.gradle
/local.properties
# IDE state — ignore the whole .idea/ folder; share project config via
# .editorconfig / build scripts instead.
/.idea/
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

# Signing keystore configuration (contains passwords!)
keystore.properties
*.jks
*.keystore
