<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/background2">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:padding="@dimen/main_screen_padding">

        <!-- Header with Logo and Wordmark at Top -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:layout_marginTop="24dp"
            android:layout_marginBottom="32dp">

            <!-- Just Wordmark Centered - Much Bigger -->
            <TextView
                android:layout_width="0dp"
                android:layout_weight="1"
                android:layout_height="wrap_content"
                android:text="@string/wordmark_text"
                android:contentDescription="@string/waterfountain_wordmark"
                android:textSize="120sp"
                android:textColor="@android:color/white"
                android:fontFamily="@font/century_gothic_bold"
                android:maxLines="1"
                android:singleLine="true"
                android:ellipsize="end"
                android:gravity="center"
                android:letterSpacing="-0.05"
                android:includeFontPadding="false"
                android:paddingHorizontal="16dp"
                android:shadowColor="#30000000"
                android:shadowDx="0"
                android:shadowDy="3"
                android:shadowRadius="6" />

        </LinearLayout>

        <!-- Main Content Area - Centered Vertically -->
        <LinearLayout
            android:id="@+id/mainContent"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:orientation="vertical"
            android:gravity="center"
            android:clipChildren="false"
            android:clipToPadding="false">

            <!-- Free Cold Water Text -->
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Free Cold Water"
                android:textSize="64sp"
                android:textColor="#555555"
                android:fontFamily="@font/inter"
                android:gravity="center"
                android:includeFontPadding="false"
                android:layout_marginBottom="8dp"/>

            <!-- Yes, It's Free! Text (Extra Bold) -->
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Yes, It's Free!"
                android:textSize="64sp"
                android:textColor="#555555"
                android:fontFamily="@font/inter"
                android:textStyle="bold"
                android:gravity="center"
                android:includeFontPadding="false"
                android:layout_marginBottom="0dp"
                android:shadowColor="#40000000"
                android:shadowDx="0"
                android:shadowDy="1"
                android:shadowRadius="2"/>

            <!-- Can Image -->
            <ImageView
                android:id="@+id/canImage"
                android:layout_width="1000dp"
                android:layout_height="1000dp"
                android:src="@drawable/can"
                android:contentDescription="Water can"
                android:scaleType="centerCrop"
                android:layout_marginTop="0dp"
                android:layout_marginBottom="0dp"
                android:adjustViewBounds="false"/>

            <!-- Tap to start instruction text -->
            <TextView
                android:id="@+id/instructionText"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Tap to start"
                android:textSize="56sp"
                android:textColor="#555555"
                android:fontFamily="@font/inter"
                android:gravity="center"
                android:letterSpacing="0.00"
                android:includeFontPadding="false"/>

        </LinearLayout>

    </LinearLayout>

    <!-- Modal Overlay (initially hidden) -->
    <FrameLayout
        android:id="@+id/modalOverlay"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#C0000000"
        android:visibility="gone"
        android:clickable="true">

        <!-- Modal Content -->
        <LinearLayout
            android:id="@+id/modalContent"
            android:layout_width="@dimen/modal_width"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:background="@drawable/modal_background"
            android:orientation="vertical"
            android:padding="@dimen/modal_padding"
            android:gravity="center"
            android:elevation="16dp">

            <!-- Header with Title and Close Button -->
            <RelativeLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginBottom="@dimen/margin_large">

                <!-- Modal Title - Centered -->
                <TextView
                    android:id="@+id/modalTitle"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_centerInParent="true"
                    android:text="How It Works"
                    android:textSize="@dimen/modal_title_size"
                    android:textColor="#555555"
                    android:fontFamily="@font/inter"
                    android:textStyle="bold"
                    android:gravity="center"
                    android:letterSpacing="0.00"/>

                <!-- Close X Button - Top Right -->
                <ImageButton
                    android:id="@+id/closeModalButton"
                    android:layout_width="48dp"
                    android:layout_height="48dp"
                    android:layout_alignParentEnd="true"
                    android:layout_centerVertical="true"
                    android:background="?android:attr/selectableItemBackgroundBorderless"
                    android:contentDescription="Close"
                    android:src="@android:drawable/ic_menu_close_clear_cancel"
                    android:tint="#555555"
                    android:padding="12dp"/>

            </RelativeLayout>

            <!-- Modal Message -->
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Local businesses sponsor your water by advertising on the can. You get free hydration + exclusive discounts!\n\n🔒 Your Number\nWe only use your phone number to keep things fair - limiting 2 free waters per day per person. Your number is encrypted and never sold.\n\n✨ That's It!\nTap to start and enjoy your free water."
                android:textSize="@dimen/modal_text_size"
                android:textColor="#555555"
                android:fontFamily="@font/inter"
                android:gravity="center"
                android:lineSpacingExtra="8dp"
                android:lineSpacingMultiplier="1.2"
                android:paddingHorizontal="16dp"
                android:layout_marginBottom="@dimen/margin_large"/>

        </LinearLayout>

    </FrameLayout>

</FrameLayout>
